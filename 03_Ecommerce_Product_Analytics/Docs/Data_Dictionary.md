# Data Dictionary & KPI Rules

## Ecommerce KPIs
- Cart Abandonment Rate = 1 - (SUM(purchased)/SUM(added_to_cart))
- Conversion Rate = SUM(purchased)/COUNT(session_id)
- AOV = AVG(order_value) over purchased sessions
- Revenue = SUM(order_value)
