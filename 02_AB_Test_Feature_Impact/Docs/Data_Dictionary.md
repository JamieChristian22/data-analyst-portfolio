# Data Dictionary & KPI Rules

## Experiment KPIs
- Conversion Rate = SUM(converted)/COUNT(session_id)
- Relative Lift = (CR_variant - CR_control)/CR_control
- Bounce Rate = SUM(bounce)/COUNT(session_id)
- Revenue per Session = SUM(revenue)/COUNT(session_id)
